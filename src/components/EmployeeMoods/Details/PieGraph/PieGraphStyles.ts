import { makeStyles } from '@material-ui/core/styles';

const styles = () =>
  makeStyles({
    customTooltip: {
      backgroundColor: '#ffffff',
    },
    label: {
      color: '#696969',
      margin: 20,
    },
  });

export default styles;
