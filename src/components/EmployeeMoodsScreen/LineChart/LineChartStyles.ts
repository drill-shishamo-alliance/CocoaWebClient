import { makeStyles } from '@material-ui/core/styles';

const styles = () =>
  makeStyles({
    chartPosition: {
      flexBasis: '80%',
    },
    iconSize: {
      fontSize: 20,
    },
  });

export default styles;
